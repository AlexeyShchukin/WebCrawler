# Content Service

Consumes fetched HTML pages, extracts text, links, and keywords, persists content data, and indexes pages in Elasticsearch. It also exposes internal read-only search and page-detail endpoints for the API Service.
