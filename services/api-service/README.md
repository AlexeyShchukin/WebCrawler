# API Service

Public FastAPI service for crawl creation, crawl status, page metadata, and full-text search.

The service calls Frontier for crawl operations and Content for search and page details. It does not connect directly to another service's database or to Elasticsearch.
